require "rails_helper"

RSpec.describe [:CLASSNAME:] do
  let(:service) { described_class.new(params) }
  let(:params) { }

  describe "#call" do
    it "description" do
      pending "#{__FILE__}"
    end
  end
end
